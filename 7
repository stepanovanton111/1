def factorial_iterative(n):
    if not isinstance(n, int) or n < 0:
        raise ValueError("n должно быть неотрицательным целым числом")
    if n == 0:
        return 1
    result = 1
    for i in range(1, n + 1):
        result *= i
    return result

n = int(input("Введите неотрицательное целое число: "))
print(f"Факториал {n} равен: {factorial_iterative(n)}")
